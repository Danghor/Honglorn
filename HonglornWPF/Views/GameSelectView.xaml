<UserControl x:Class="HonglornWPF.Views.GameSelectView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HonglornWPF"
             xmlns:viewModels="clr-namespace:HonglornWPF.ViewModels"
             mc:Ignorable="d"
              d:DataContext="{d:DesignInstance viewModels:GameSelectViewModel}">
    <StackPanel>
        <DataGrid ItemsSource="{Binding Wrappers}" SelectedItem="{Binding CurrentWrapper}" />
        <Button Content="Edit Game" Command="{Binding SelectGameCommand}" IsEnabled="{Binding CurrentWrapper, Converter={StaticResource ObjectNotNullToBoolConverter}}" />
    </StackPanel>
</UserControl>