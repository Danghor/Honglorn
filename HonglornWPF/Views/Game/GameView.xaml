<UserControl x:Class="HonglornWPF.Views.Game.GameView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HonglornWPF.Views.Game"
             xmlns:viewModels="clr-namespace:HonglornWPF.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <viewModels:GameViewModel />
    </UserControl.DataContext>
    <StackPanel>
        <ContentControl Content="{Binding CurrentGameViewModel}">
            <ContentControl.Resources>
                <!--<DataTemplate DataType="{x:Type viewModels:ClassListViewModel}">
                <masterData:ClassListView />
            </DataTemplate>-->
            </ContentControl.Resources>
        </ContentControl>
        <Button Content="Game Select" Command="{Binding GameSelectCommand}" />
    </StackPanel>
</UserControl>
